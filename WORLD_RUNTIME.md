# WORLD 실행과 구성

[WORLD_CONSTITUTION.md](WORLD_CONSTITUTION.md)가 최상위 제품 사양입니다. 원문은 변경하지 않았으며 엔진 검증과 공통 프롬프트에 제약을 반영했습니다. 기존의 부활 금지·소유권·동일 행동 반복 제한도 유지합니다.

## 실행

1. `.env.example`을 참고해 서버 환경을 설정합니다. 최초 관리자 생성에는 `ADMIN_SEED_USERNAME`, `ADMIN_SEED_PASSWORD`가 필요합니다. 기존 계정의 비밀번호는 이 설정으로 변경되지 않습니다.
2. 개발에서는 `npm run server`와 `npm run dev`를 각각 실행합니다. 배포에서는 `npm run build` 후 `npm start`를 사용합니다. DB 파일이 있는 저장 공간을 유지해야 합니다.
3. 관리자 아이디로 로그인합니다. `/admin/world/builder`에서 규칙·환경·지도·캐릭터·관계를 저장한 뒤 START WORLD를 누릅니다.
4. `/admin/world`에서 갱신 간격, 주기당 판단 인원, 호출 예산, 일시정지/재개, 판정 기록을 확인합니다. `/admin/world/rule-presets`에서 세계별 추가 규칙을 편집합니다.

WORLD 관리자 API는 세션 쿠키와 ADMIN 권한을 요구합니다. 변경 요청에는 `x-world-admin: 1`과 같은 Origin이 필요합니다. 프록시는 원래 Host를 보존해야 합니다.

## 데모와 실제 AI

기본값 `AI_WORLD_DEMO_MODE=true`에서는 실제 엔진으로 이동·소비·수면을 진행하고 AI 요청은 보내지 않습니다. 인물의 선택은 단순한 데모 정책입니다. 최초 샘플 세계는 정적인 디자인 미리보기이며, 새 세계를 시작하면 엔진이 작동합니다.

실제 AI를 사용하려면 서버에 `AI_WORLD_DEMO_MODE=false`, 필요한 provider 키와 모델을 설정하고 재시작한 뒤 **새 세계**를 시작합니다. 실행 모드와 설계·규칙은 시작 시 고정됩니다. 운영 환경의 기존 provider 한도 확인 조건도 적용됩니다. 이번 구현에서는 실제 키나 `.env`를 변경하지 않았고 유료 요청을 보내지 않았습니다.

캐릭터는 자신의 성격·욕구·감정·기억·관계·관찰·알고 있는 정보만 받습니다. AI는 구조화된 행동을 제안하고 엔진의 코드 검증과 별도 규칙 판정을 거칩니다. 판정 모델은 숨겨진 설정을 볼 수 있지만 그 내용이나 사유를 공개 피드로 보내지 않습니다. 모델 가격이 등록되어 있어야 호출할 수 있습니다.

## 시간, 행동과 비용

| 설정 | 기본값 | 의미 |
| --- | --- | --- |
| `MAX_ACTIVE_CHARACTERS` | 10 | 생성·시작·재개 시 WORLD 인원 한도 |
| `WORLD_MINUTES_PER_TICK` | 1 | 갱신 1회에 흐르는 세계 시간(분) |
| `WORLD_DECISIONS_PER_CYCLE` | 2 | 주기당 판단 인원의 초기값; 관리자에서 조정 가능 |
| `WORLD_DECISION_COOLDOWN_MS` | 30000 | 실제 AI 판단 배치 사이의 최소 대기 시간 |
| `WORLD_CHAPTER_MINUTES` | 60 | 완료 사건을 챕터로 묶는 세계 시간 간격 |

세계 갱신 간격은 생성 Wizard 또는 관리자에서 설정합니다. AI 응답을 기다리는 동안에도 시간을 진행합니다. 각 캐릭터의 `nextDecisionAt`, 현재 행동, 관련 사건, 상태를 기준으로 필요한 캐릭터만 판단합니다. 이동·수면 중에는 재호출하지 않으며 직접 말을 걸거나 주변에 중대한 위험이 발생하면 관련 캐릭터만 일찍 판단합니다.

이동은 경로의 `travelTime`이 지난 뒤 완료됩니다. 시작 시와 완료 시 위치·경로·신체·수용 인원을 검증합니다. 도착 전에는 목적지 자원을 사용할 수 없습니다. 이동 중인 인물에게 출발 장소에서 물건을 주거나 대화할 수도 없습니다.

60분 단위의 생존 욕구·피로·스트레스·위험 반응, 휴식과 수면의 회복은 코드로 계산합니다. HUMAN STATE와 EMOTION은 1~10 정수입니다. 건강 수치는 **1=안정, 10=치명적 상태**입니다. 욕구·감정 수치가 특정 행동을 강제로 실행하지 않습니다.

AI 판단은 제안과 판정으로 최대 2회 요청을 사용합니다. 코드 검증에서 거절되면 판정 요청은 생략합니다. 주간·월간 금액 예산은 기존 AI Community와 공유하며 요청 전에 예상 금액을 예약합니다. 실패·알 수 없는 사용량은 보수적으로 정산하고 자동 재시도하지 않습니다.

예산 소진·provider 장애·잘못된 응답은 **새 AI 판단만 중단**합니다. 세계 시간과 이미 진행 중인 행동은 계속됩니다. 문제를 해결한 뒤 관리자에서 재개합니다. 시즌 전체 일시정지는 세계 시간도 멈춥니다.

## 관리자가 정하는 실제 세계

- 좌표와 연결, 이동 시간, 봉쇄 여부, 수용 인원, 장소별 자원과 물건을 설정합니다. 실행 미니맵은 이 데이터와 실제 이동 진행률을 사용합니다.
- 세계 초기 자원은 첫 번째 장소에 배치합니다. `food`와 `water`는 소비할 수 있고 각각 수량이 감소합니다. 같은 위치에 같은 자원 키를 중복 지정하면 시작을 거절합니다.
- 물건 이름/ID는 고유합니다. 소지품으로 지정하면 해당 인물 소유로 시작하며 장소 물건과 중복 생성하지 않습니다. 가져오기와 전달은 기존 물건의 위치만 바꿉니다.
- **HIDDEN WORLD TRUTH**는 판정 전용 비밀 배경입니다. 자동으로 캐릭터 지식이 되지 않습니다.
- **탐색으로 발견할 수 있는 진실**은 내용·발견 장소·선택적 공개 장소를 지정합니다. 지도 장소를 먼저 저장한 다음 지정합니다. 그 장소의 탐색을 완료한 인물만 획득합니다. 다른 인물에게 공유한 내용은 출처가 있는 미확인 보고로 저장합니다.
- 관계는 A→B와 B→A를 독립적으로 저장합니다. 물건을 받은 경험 등 실제 원인에 따라 변화하며 기억에는 근거 사건 ID를 남깁니다.
- 관리자 공지는 공지 행위로만 기록합니다. 자유 입력 문장으로 자원·시설·인물 상태를 바꾸지 않습니다.

## LIVE / TEXT / 저장

LIVE는 최근 30개 사건부터 표시하고 과거 기록을 추가로 불러옵니다. 세계 시간, 사건 유형, 행동 시작·완료·중단을 구분합니다. 과거를 읽을 때 새 기록을 별도로 쌓고 최신 위치로 이동할 때 보여줍니다. SSE를 우선 사용하고 연결이 끊어지면 조회로 보완합니다.

TEXT는 완료된 사건을 DAY/Chapter 단위로 묶습니다. 진행 중 행동을 완료된 결과로 서술하지 않습니다. 현재 Narrator는 확정 사건만 장소별 문단으로 정리하는 **결정적 서술기**이며 별도 소설 생성 LLM을 호출하지 않습니다. 원인 없는 극적 장면을 추가하지 않습니다. 복귀 안내의 행동·주요 사건 수는 마지막 관전 이후의 실제 로그에서 계산합니다.

`world_runtime_checkpoint`에 설계·엔진·진행 중 행동·기억·판단 일정·예산을 저장합니다. `world_event_journal`은 사건을 계속 보존하므로 메모리의 최근 500개 제한 뒤에도 조회할 수 있습니다. 서버 재시작 시 마지막 상태를 **일시정지**로 복구합니다. 서버 프로세스가 꺼진 동안의 사건을 만들어 따라잡지는 않습니다.

브라우저 접속 여부는 시뮬레이션에 영향을 주지 않습니다. 비공개 세계 조회는 관리자만 가능하며 공개 구독은 비공개 세계로 교체할 때 종료합니다. 공개 API에는 비밀 지식·개인 메모·숨겨진 진실·미발견 장소·모델 판정 내용을 내보내지 않습니다.

## 지원 범위와 검증

실행 행동: MOVE, SPEAK, TAKE_ITEM, GIVE_ITEM, EAT, DRINK, REST, SLEEP, EXPLORE, SHARE_INFO, WAIT, OBSERVE, INTERACT, COOPERATE. 마지막 두 행동은 시도 기록만 남기며 시설 수리·자원 생산 성공을 만들어내지 않습니다. 효과가 정의되지 않은 ATTACK/USE_ITEM은 거절합니다. 성적 상호작용은 지원하지 않아 다른 인물의 동의를 대신 확정하는 경로도 없습니다. 자유 형식 통행 조건은 자동 충족시키지 않고 이동을 거절합니다. 자동 GM 세계 확장은 활성화하지 않습니다.

자유 서술형 규칙·종료 조건은 모델 판정이므로 코드 검증과 동일한 보장은 아닙니다. 최대 일수·전원 사망 종료는 코드로 판단합니다. 한 서버 프로세스에서 현재 세계 하나를 실행합니다.

| 사양 | 구현 근거 |
| --- | --- |
| §§0–13, 35–47: 상태·검증·인과·보존 | worldEngine, worldValidator, stateTransition, worldLaunch |
| §§3–4, 24–29: 비밀·지식·관계·기억 | knowledgeFilter, publicView, 발견/공유·방향별 관계·근거 사건 |
| §§14–23, 30–32: 상태·감정·욕구와 선택 분리 | 초기 상태 검증, 시간별 상태 갱신, 최소 프롬프트 |
| §§33–34, 48–51, 57–58: 활성화·비용·시간 분리 | 인원 설정, 개인 일정, 관련 사건 조기 판단, 독립 엔진 타이머 |
| §§37–40, 52–56: 사건·서술·LIVE/TEXT·복귀 | 영속 사건 기록, 완료 챕터, LIVE 피드, 실제 복귀 집계 |

검증 명령: `npm test`, `npm run typecheck`, `npm run build`, `npx playwright test tests/world-admin.spec.ts tests/world-experience.spec.ts tests/world-live.spec.ts --workers=2`. 테스트에는 유료 API 호출이 없습니다.
