# 관리자 제작 시스템 개편 결과

기준 문서: `AI_WORLD_관리자_제작시스템_전면개편 (1).txt`.
운영 주소: https://web-production-0a945.up.railway.app/admin/world/builder

## 제작 화면

세계 기본 설정 → 환경 → 장소 → 자원 및 아이템 → 사건/숨겨진 진실 → 캐릭터 → 관계 → 시뮬레이션 → 검토 → 시작의 10단계로 교체했다.
단계 이동(상단 단계 버튼 포함) 시 전체 초안을 한 DB 트랜잭션으로 저장한다. 저장에 실패하면 이동하지 않고 입력을 유지한다. 저장 상태와 현재 단계를 표시하며, 저장하지 않은 상태에서 브라우저를 닫으면 경고한다. 자동으로 API를 호출하거나 세계를 시작하지 않는다.

- 필수 이름은 WORLD 이름 하나. 시즌은 기본 `Season 1`, 고급 설정에서 수정.
- 소개는 선택 사항이며 표시용. AI 추천으로 생성하고 적용하거나 직접 수정할 수 있다. 비어 있으면 관전 화면에서 배경을 대신 표시한다.
- 장르, 성격, 장점, 약점은 복수 태그와 사용자 정의 추가.
- 장소는 카드와 연결 체크박스. 좌표는 기본 입력에서 제거. 기존 좌표는 보존.
- 자원은 장소별 카드. 아이템은 이름/종류/수량/보유 위치를 따로 저장.
- 캐릭터는 직접 생성과 AI 생성 후 편집. 영어 상태 키는 한국어 상태명으로 교체.
- 관계는 여섯 가지 라벨만 선택. 내부 수치는 상수에서 초기화.
- 숫자는 임시 빈 값, 전체 선택, 삭제, 직접 입력, 방향키, ± 버튼을 지원. 경계값은 blur 또는 저장 때 확인.
- 전체 캐릭터(최대 100명)와 동시 활성 인원(서버 허용 한도 이내)은 별개.
- 숨겨진 진실의 조건과 세부 기후 설정은 고급 설정으로 이동.

## DB 및 호환성

`world_studio_config`를 추가하는 재실행 가능한 migration을 사용한다. 기존 테이블·계정·세계 데이터를 삭제하지 않는다. 새 설정은 JSON으로 저장하고 실행 시 동결한 설정/상태를 기존 `world_runtime_checkpoint`에 기록한다. 사건은 기존 `world_event_journal`에 보존한다.

기존 초안을 처음 열면 자유 입력 성격을 사용자 정의 태그로, 기존 소지품을 수량 1 아이템으로, 전역 자원을 첫 장소의 자원으로 매핑한다. 구형 숨겨진 진실/발견 정보와 사건 원문도 보존한다. 기존 자유 입력 종료 문장은 새 엔진의 자동 조건으로 오인하지 않도록 최종 검토에서 재설정을 안내한다. 이미 실행 중인 구형 WORLD에는 새 설정을 임의로 적용하지 않는다.

## 상태와 엔진

- `engine.weather`: 기후, 현재/이전 날씨, 온도, 습도, 강수량, 풍속, 지속 일수, DAY. 시드와 이전 날씨를 사용하고 DAY 경계마다 계산한다.
- 캐릭터: 젖음, 추위/더위 노출, 실외 노출 일수, 피부 상태, 감염 위험을 누적한다. 실내에서는 일부 값이 서서히 회복된다. 건강/체력/허기/갈증/외로움과 기존 피로·감정도 행동 맥락으로 전달한다.
- 장소: 실외 여부, 전력, 온도, 침수, 접근 가능 상태. 장기 폭우는 실외 장소를 침수시키며, 침수된 목적지는 이동 검증에서 거부한다. 비는 이동 시간을 늘린다.
- 식량/식수 섭취와 자원 작업은 실제 수량을 소비한다. 야외 폭우·고온은 식량에, 추위의 난방은 연료에 영향을 준다.
- 예약 사건: DAY/시각, 대상 장소, 공개 범위, 전력·침수·자원·목표 효과를 엔진이 실행하고 로그를 남긴다. 같은 사건은 한 번만 적용한다. 자연어 설명만으로 임의의 STATE 변경을 허용하지 않는다.
- 종료: DAY, 생존자 수, 장소 도달, 등록된 목표 달성, 등록된 사건, 전원 사망을 AND/OR로 검사한다. 최대 일수는 별도의 상한이다. 새 WORLD는 AI의 `ended` 응답만으로 종료하지 않는다.
- 한 DAY 테스트: 시작 DAY 1, 최대 일수 1. 시작 시각과 무관하게 DAY 2 자정 경계에서 멈춘다.
- 아이템: 실제 보유자와 수량을 유지한다. 습득/전달/내려놓기/소모/환경 손실을 처리하며 없는 물건을 사용하지 못한다. 일반 물품에는 임의 효과가 없고 음식·물·의약품·연료·도구처럼 정의된 사용만 허용한다.
- 관계: 방향별 신뢰/호감/적대/경쟁과 상호작용 횟수. 전달/협력은 긍정 변화, 목격된 대량 식량·물품 독점은 부정 변화. 누적 값으로 라벨을 바꾸고 연인은 양방향 감정·상호작용·성별/지향 조건이 충족되어야 한다.
- 진실: 초기 인지로 지정된 인물 외에는 엔진 내부에만 보관. 탐색 위치, 아이템, 선행 사건 조건을 충족한 뒤 해당 인물의 지식으로 이동한다. 공개 응답에서 원문과 미래 사건 설정을 제거한다.
- 활성 인원: 긴 행동 중/수면 중/판단 시각 전 인물을 제외하고 위험·욕구·관계·대기 시간을 고려한다. 비활성 인물도 상태/기억/소지품은 유지한다.

장르/배경/성격/직업은 AGENT 맥락에 들어간다. 직업이 성공을 보장하는 능력치나 치트는 아니다. NARRATOR는 확인된 사건을 문장으로 묶고 장르에 따라 기록 제목을 조정하는 기존의 결정적 렌더러를 유지한다. 별도 유료 NARRATOR 모델 호출을 추가한 것은 아니다.

## AI 추천

소개, 장소, 자원, 직업, 초기 사건, 숨겨진 진실, 종료 조건을 제안한다. 관리자가 적용해야 초안이 바뀐다. 설정에 없는 사실을 곧바로 실행하지 않는다. 적용한 제안도 해당 단계에서 수량·효과·대상을 확인한다. 실패 시 가짜 AI 결과로 대체하지 않으며 기존 선불/월간/주간 예산 예약·정산을 사용한다. 테스트 모드에서는 명시적인 고정 제안을 사용한다.

API 응답은 서버에서 JSON 형식을 검증한다. 참고: [OpenAI Structured Outputs](https://developers.openai.com/api/docs/guides/structured-outputs).

## 필드 전수 분류

| 화면 입력 | 분류 | 사용처 |
|---|---|---|
| WORLD 이름 | 실행 메타데이터 및 표시 | 시즌/시작 사건/캐릭터 생성 맥락 |
| 장르/배경 | 시뮬레이션 | WORLD CONTEXT, AGENT, 기록 형식 |
| 소개/시즌 이름 | 표시용 | 관전자 소개와 시즌 제목 |
| 규칙 프리셋 | 시뮬레이션 | 시작 시 스냅샷, AGENT/JUDGE |
| 기후/시작 날씨/온도/기후 확률 | 시뮬레이션 | 일별 날씨, 누적 노출, 자원/이동 |
| 장소 이름/유형/설명/연결/발견 여부/수용 인원 | 시뮬레이션 | 위치 그래프, AGENT 관측, 이동 검증 |
| 자원 종류/이름/수량/단위/장소 | 시뮬레이션 | 실제 재고, 섭취/작업/날씨/사건 |
| 아이템 이름/종류/수량/보유 위치 | 시뮬레이션 | 실제 객체와 INVENTORY |
| 사건 이름/내용 | 사건 설명 | 등록 사건에 붙는 텍스트. 단독으로 현실 변경하지 않음 |
| 사건 DAY/시각/효과/장소/수량/목표/공개 범위 | 시뮬레이션 | 결정적 스케줄과 STATE 변경 |
| 숨겨진 진실 내용/발견 가능 여부/장소/물품/사건/초기 인지 인물 | 시뮬레이션 | 조건 검증 후 지식 획득 |
| 캐릭터 이름/나이/직업/성격/장점/약점/배경/목표/개인 정보/초기 지식 | 시뮬레이션 | 개별 AGENT 맥락과 지식 |
| 성별/성적 지향 | 시뮬레이션 | 개별 맥락과 연애 관계 조건 |
| 신체·정신 상태 | 시뮬레이션 | 엔진 상태와 개별 판단 맥락 |
| 초기 위치/소지품/관계 | 시뮬레이션 | 실제 배치·보유·방향별 감정 |
| 캐릭터 목표 수 | 제작 | AI 생성 수량. 실제 인원이 다르면 검토 경고 |
| 동시 활성 인원/Tick 간격/Tick당 세계 시간 | 시뮬레이션 | 스케줄러와 세계 시간 |
| 시작 DAY/시각/최대 일수/종료 조건 | 시뮬레이션 | 초기 시각과 종료 검사 |

**UI에 존재하지만 실제 시뮬레이션에서는 사용되지 않는 설정값**

- 소개와 시즌 이름은 의도적인 표시용이다. 소개는 AI 행동 전제에서 제외했다.
- AI 추천의 미적용 제안은 실행 데이터가 아니다.
- ‘알림만’ 사건은 로그만 만들고 물리 상태를 바꾸지 않는다. 선택지에 이를 표시한다.
- 전체 캐릭터 목표 수는 제작 보조 값이며, 실제 실행 인원은 저장된 캐릭터 수다.
- 구형 자유 입력 종료 문장은 새 제작 시스템에서 자동 실행하지 않는다. 경고와 원문을 보존하고 구조화 조건으로 전환하도록 안내한다.
- 구형 외형/임의 모델/좌표/자유 입력 시설 상태/불명확한 사건 텍스트 등 기본 입력을 제거했다. 기존 저장 데이터는 삭제하지 않았다.

새 UI의 실행 설정 중 저장만 하고 엔진/AGENT에 전달하지 않는 필드는 없다. 다만 자연어를 임의 코드로 바꾸는 기능, 모든 질병·재난·제작 행동을 포괄하는 물리 모델, 거짓말/배신을 언어만으로 확정하는 판정은 구현하지 않는다. 현재 지원 효과와 상태 범위 안에서 검증한다.

## 검증

- 단위/통합 테스트: 91개 통과.
- 데스크톱/모바일 관리자 인증·10단계 편집·숫자·재접속·최종 검토: 4개 통과.
- 타입 검사, 빌드 및 공개 번들 비밀정보 검사 통과.
- `node --experimental-strip-types tool/test-studio-island.ts`: 독립 SQLite에 실제 테스트 아일랜드 생성, 204 Tick / DAY 3까지 진행 후 생존자 소진으로 종료, 유료 호출 0회. 결과는 `data/studio-tests/latest-report.json`.
- 별도 통제 실험에서 4일간 우천 노출 누적/실내 회복, 조건부 발견, 자원 감소, 관계 비대칭, 날짜 경계 종료를 확인했다. 노출 실험은 굶주림 영향을 분리하기 위해 공급/휴식 상태를 유지했다.
