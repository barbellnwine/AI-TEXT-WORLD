# 점검 결과 — 2026-09-18

## 키 보호·비용 악용 방어 후속 작업

- 사용자 목표: 월 5만 원 운영, 초과 상황 포함 6만 원까지 감당. 추가 1만 원은 비상 여유이며 평소 유료 호출 한도는 확대하지 않았습니다.
- 운영 서버에서 외부 공급자 지출 차단 확인 전 유료 호출을 잠그는 설정을 추가했습니다. 실제 계정 한도를 자동 확인/설정하는 기능은 아니며, 계정 연결 후 확인이 필요합니다.
- 관리자 설정의 예산 상한/안전 여유·환율·단가 하한, 유료 생성 5분 간격(재시작 후 유지), 지정 공급자 URL만 허용, 요청/응답 크기 제한과 본문 타임아웃, 관리자 인증 강화, 운영 요청량 제한, CSP, 민감 경로 404 처리를 추가했습니다.
- 공개 빌드에 API 키/관리자 비밀값이 섞이면 빌드를 실패시키는 검사도 추가했습니다. `.env`는 Git에서 제외되어 있으며 실제 키·운영 DB는 수정하지 않았습니다.
- 계정 측 실제 한도·자동충전 해제는 아직 적용되지 않았습니다. 전체 서버/결제 계정 침해, 외부 키 악용과 최종 청구액까지 앱만으로 보장하지 않습니다. 적용 계획과 공식 근거는 `SECURITY_BUDGET.md`에 있습니다.

## 월 5만 원 운영 구성 후속 작업

- 작업 대상은 `D:\ai-amnesty-protocol`입니다. 낭만상점 프로젝트는 변경하지 않았습니다.
- 웹 운영 월 1만 원/API 월 4만 원, 무료 기본 주소 요청에 맞춰 Railway Docker 배포 구성을 추가했습니다. 기존 Render 구성은 대안으로 남겼습니다.
- 한국 시간 기준 월간 예산 장부를 추가했습니다. 주간 한도와 함께 적용하며, 기존 유료 호출 로그를 최초 이전하고 관리자 화면에 월간 사용액·예산을 표시합니다.
- 배포 기본값: 월 40,000원·주 10,000원·여유 20%, 5분 간격, 자동 재시도 0회. 타임아웃·응답 오류도 추정 비용을 장부에 보수적으로 남깁니다.
- 로컬의 기존 `.env` 및 운영 DB는 변경하지 않았습니다. 기존 설치의 DB 설정과 `.env` 값은 새 기본값보다 우선하므로 실제 운영 전 확인이 필요합니다.
- Railway CLI는 실행 가능하지만 계정이 아직 연결되지 않았습니다. 사용자에게 계정 생성과 CLI 연결을 요청했습니다. 공개 사이트·볼륨·결제 한도는 아직 생성/적용하지 않았습니다.
- 자세한 배포와 비용 제한 절차는 `DEPLOYMENT.md`를 참고하세요.
- 최종 검증: 단위·예산 테스트 29개, PC·모바일 브라우저 테스트 14개(중복 4개 제외), 재시작 보존 테스트 1개, 타입 검사 및 웹 빌드 통과. Docker 엔진이 없어 로컬 컨테이너 빌드는 실행하지 못했으며 Railway 실제 배포 검증은 계정 연결 후 진행해야 합니다.
- 재시작 보존 테스트의 DB 경로를 OS 임시 폴더로 분리했습니다. Playwright가 `test-results`를 비울 때 테스트 DB까지 지우던 병렬 실행 충돌을 수정했습니다.

## 구현 상태

- 면죄부: 평가 JSON 파싱, 점수 보정, 티어 계산, 발급, 복사 대체 경로, 재심, 초기화가 구현되어 있고 PC·모바일 브라우저 검증을 통과했습니다.
- AI 커뮤니티: 10개 계정, SQLite 저장, 데모 생성, 스케줄러, 피드·필터·상세·프로필, 4개 언어 UI, 관리자 인증·설정·로그가 구현되어 있습니다. 데모 기반 통합 동작을 확인한 초기 실험 버전입니다.

## 수정한 오류

1. 관리자 페이지가 LanguageProvider 없이 CommunityFooter를 렌더링하여 빈 화면이 되는 오류. PC·모바일에서 재현 후 수정했습니다.
2. 대기 중인 계정이나 API 키가 없는 계정이 계속 선택되어 다른 계정의 실행까지 막히는 문제. 실행 가능한 계정을 우선 선택하고 실패한 호출도 실행 순서에 반영했습니다.
3. 잘못 인코딩된 URL의 예외가 라우터 밖으로 전파되는 문제. 400 응답으로 처리합니다.
4. 빌드 디렉터리 자체를 파일로 읽는 정적 파일 처리 오류. 파일 여부와 경로 범위를 확인하고 최상위 요청 예외를 처리합니다.
5. 관리자 모드·활성화 값의 잘못된 타입, 음수 예산, 잘못된 환율·안전 여유율·단가가 저장되는 문제. 전체 입력을 검증한 뒤 반영합니다. 없는 계정의 변경은 404로 응답합니다.
6. 소수·무한대·음수 페이지 조회값이 SQL 오류나 예상 밖 조회를 만드는 문제. 피드와 로그에 정수 범위를 적용했습니다.
7. 공개 계정 API가 내부 provider/model을 포함하던 문제. 공개 응답과 공개 타입에서 제거했습니다.

## 검증

- `npm test`: 22개 통과.
- `npm run test:e2e`: PC·360px 모바일 6개 통과. 면죄부 전체 흐름과 커뮤니티 데모 생성·필터·상세·프로필·언어 유지·관리자 접속을 포함합니다.
- `npm run typecheck`, `npm run build`: 통과.
- 커뮤니티 브라우저 테스트는 별도 포트의 메모리 DB와 데모 모드를 사용합니다. 기존 `.env` 및 실제 DB를 변경하지 않았고 유료 API를 호출하지 않았습니다.

## 남은 범위와 한계

- 실제 OpenAI/Anthropic 응답, 실제 과금 및 장시간 연속 운영은 이번 점검에서 검증하지 않았습니다.
- 현재 모델에 주는 공개 맥락은 최근 글의 제목·작성자·시각뿐입니다(`server/domain/promptBuilder.ts`). 글 본문과 기존 댓글이 전달되지 않아 본문을 읽고 이어지는 논쟁이나 대댓글 대화까지 구현된 상태는 아닙니다.
- 공개 피드는 현재 최근 20개 조회 중심이며 이전 글을 더 불러오는 UI는 없습니다(`src/ai-community/CommunityPage.tsx`). 후속 개선으로 15초 자동 갱신을 추가했습니다.
- 이 결과는 위 시나리오와 코드 검토 범위의 확인이며 전체 배포 환경에 대한 무결함 보증은 아닙니다.

## 후속 업데이트 — 커뮤니티 피드 자동 갱신

- 요청에 따라 면죄부 메인 화면의 `100% 풍자 · 0% 법적 효력` 문구를 제거했습니다.
- 커뮤니티 목록에서 15초마다 최신 활동·운영 상태·계정 정보를 확인합니다. 현재 필터를 유지하고 마지막 확인 시각을 표시합니다.
- 숨긴 탭은 주기 조회를 멈추고 돌아오면 다시 확인합니다. 필터 전환이나 화면 이탈 시 이전 요청을 취소합니다.
- 일시적인 조회 실패에는 기존 목록을 유지하고 자동으로 재시도합니다. 늦게 도착한 이전 필터 응답은 적용하지 않습니다.
- 이 기능은 공개 조회 API만 사용하며 AI 생성 호출을 추가하지 않습니다.
- 후속 검증: 단위 테스트 22개, PC·모바일 브라우저 테스트 10개, 타입 검사·빌드 통과. 브라우저 테스트에 자동 갱신·연결 복구·숨김 탭·필터 경합 시나리오를 추가했습니다.

## 모바일 대응 및 공개 준비

- 모바일 메뉴를 분리된 버튼으로 배치하고 입력창 글자 크기를 16px, 주요 터치 영역을 최소 44px로 조정했습니다. 관리자 표는 화면 내부에서 가로 스크롤되며 전체 페이지를 밀어내지 않습니다.
- 화면 이동 시 스크롤 위치와 커뮤니티 본문 바로가기 오류를 수정했습니다. 관리자 설정 저장 실패도 화면에서 안내합니다.
- Node.js 24 운영 실행 명령, 상태 점검 API, 정적 자산 캐시, 종료 처리, Render 영구 디스크 배포 구성을 준비했습니다.
- 검증: 단위 테스트 22개, 운영 빌드/DB 재시작 테스트 1개, 브라우저 테스트 14개 통과(모바일 전용 검사의 데스크톱 중복 4개 제외). 타입 검사와 빌드도 통과했습니다.
- 검증 환경은 로컬 Chromium 및 터치 에뮬레이션입니다. 실제 iPhone/Safari 기기 검증은 포함하지 않았습니다.
- 인터넷 공개는 아직 하지 않았습니다. Render와 GitHub 설치·계정 연결 및 유료 호스팅 구성 확정이 필요합니다. 준비된 파일과 절차는 `DEPLOYMENT.md`를 참고하세요.
