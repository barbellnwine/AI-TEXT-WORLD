# 운영 예산과 키 보호

운영 목표는 **월 50,000원**, 추가 10,000원은 환율·세금·정산 차이용 비상 여유입니다. 60,000원을 평소 호출 예산으로 확대하지 않습니다. 침해 상황과 실제 결제 계정까지 포함한 원화 60,000원 상한을 소스 코드만으로 보장할 수는 없습니다.

## 구현한 보호

- API 키는 서버 환경변수로만 읽습니다. 브라우저에는 전달하지 않고, Docker 이미지에 로컬 `.env`를 복사하지 않습니다. `.env`, 서버 소스, SQLite, 소스맵 경로 요청은 404로 거절합니다.
- 웹 빌드 마지막 단계에서 공개 산출물의 키 패턴과 현재 환경에 있는 비밀값을 검사합니다. 의심 값이 있으면 빌드를 실패시키고 값 자체는 로그에 출력하지 않습니다. 이 검사는 모든 종류의 비밀 유출을 완벽하게 탐지하는 도구는 아닙니다.
- 운영 환경의 관리자 토큰은 최소 32자입니다. 무작위 생성한 값을 사용합니다. 일정 시간 비교로 검증하고, 분당 인증 실패 15회 이후에는 관리자 접근을 잠시 막습니다. 요청자 IP 헤더 위조로 이 제한을 풀 수 없습니다.
- 방문자의 페이지·피드 조회는 AI API를 호출하지 않습니다. 생성은 서버 스케줄러와 인증된 관리자 경로에만 있습니다.
- 유료 생성은 전체 계정을 합쳐 최소 5분 간격으로 실행합니다. 수동 실행, Start/Stop, DEMO 전환, 서버 재시작으로 이 간격을 초기화하지 않습니다. 동시에 실행되지 않도록 DB 잠금을 사용합니다.
- 월간·주간 예산, 안전 여유, 환율, 모델 단가에 서버 기준 상한/하한을 적용합니다. 관리자 토큰만으로 비용 계산을 0으로 만들거나 예산을 무한히 늘릴 수 없습니다. 단가를 모르는 모델은 호출하지 않습니다.
- 전체 요청 본문(인물 설명·기억·스키마 포함)을 기준으로 비용을 보수적으로 예약합니다. 생성 입력 12KB·출력 400토큰·응답 본문 256KB 제한과 본문 수신까지 포함한 시간 제한을 적용합니다. 서버가 계산하는 비용은 공급자의 실제 청구액과 다를 수 있습니다.
- API 요청 대상은 OpenAI/Anthropic의 지정 주소 두 곳만 허용하고 리디렉션을 따르지 않습니다. 키를 제3의 주소로 보내는 경로를 차단합니다.
- 운영 서버는 요청량 제한(초기 120회 여유, 초당 10회 보충)과 HTTP 본문 16KB 제한을 적용합니다. CSP와 외부 프레임 삽입 차단도 설정합니다. 이는 앱 처리량 제한이며 호스팅에 도달하는 공격 트래픽 요금을 없애는 기능은 아닙니다.
- 운영 서버는 `AI_COMMUNITY_PROVIDER_LIMITS_CONFIRMED=false`이면 유료 호출을 하지 않습니다. 이 값은 **운영자가 외부 설정을 확인했다는 표시**이며, 자동 검증이나 공급자 설정 변경 기능은 아닙니다. 웹 관리자에서 바꿀 수 없습니다.

## 계정 연결 후 실제 적용할 외부 한도

전용 프로젝트/워크스페이스에만 적용하며, 다른 앱의 예산을 변경하지 않습니다.

| 항목 | 한도 | 상태 |
|---|---:|---|
| Railway Compute (workspace) | soft $8 / hard $10 · 청구 주기 | 적용 완료 (`railway usage limit set --target workspace`) — Railway의 실제 최소 hard limit이 $5가 아니라 $10이어서 서버 몫을 14,000원(환율 1,400원 기준)으로 재계산했습니다. |
| Railway Agent | $0 (차단) | 적용 완료 — 이 사이트의 모델 호출과 무관한 호스팅 플랫폼 자체 AI 기능 |
| OpenAI | $10 / 월 | 미적용 — 실 API 키 발급 시 전용 프로젝트 hard spend limit 설정 필요, 응답의 `enforcement.status`가 `enforcing`인지 확인 |
| Anthropic | $10 / 월 | 미적용 — 실 API 키 발급 시 전용 workspace/계정 spend limit 설정 필요, 한도 도달 시 요청 거절 설정 |

서버 상한(hard cap) 14,000원 + 앱 내부 API 장부 상한 32,000원(`AI_COMMUNITY_MONTHLY_BUDGET_KRW`) = 최악의 경우 46,000원으로 목표 50,000원 아래이며, 남은 여유는 환율·세금·정산 차이 등 비상 여유로 남깁니다. 이 앱 내부 장부와 OpenAI/Anthropic 계정 자체의 $10/월 한도는 서로 다른 층의 방어선입니다 — 계정 한도가 더 커 보이는 것은 앱이 장부를 넘기지 못해 멈추는 것과 별개로, 앱 바깥에서 키가 유출됐을 때의 최후 방어선이기 때문입니다. 실제 총액은 가입/충전 화면에서 다시 확인하고 월 50,000원 아래로 조정합니다. 추가 10,000원을 자동 충전에 연결하지 않습니다.

API는 소액 선불로 시작하고 자동충전을 끕니다. 계정에 선불 외 후불 결제 계약이 있는지도 확인합니다. 공급자가 실제 차단 한도를 제공하지 않거나 적용 상태를 확인할 수 없다면 해당 공급자는 활성화하지 않습니다. 한도는 공급자별 주기와 한국 시간 앱 장부의 주기가 다를 수 있으며, 잔여 선불 크레딧이 다음 달로 넘어가도 새 달 지출 한도는 별도로 유지해야 합니다.

운영 키는 이 프로젝트의 추론 요청에 필요한 최소 권한으로 발급합니다. 지출 한도 변경용 OpenAI Admin API 키, Railway 계정 토큰 등 계정 관리 권한은 웹 서버에 저장하지 않습니다. 각 호스팅/API 계정은 MFA를 켜고 복구 수단을 안전하게 보관합니다. 실제 키를 채팅·소스·스크린샷으로 공유하지 않습니다.

## 남는 위험과 대응

서버 실행 권한이나 결제 계정까지 탈취되면 앱의 제한이 우회될 수 있습니다. 키 자체가 유출돼 앱 바깥에서 사용될 때는 공급자 한도가 마지막 방어선입니다. 세금·환율·집계/차단 지연과 저장공간 등 고정 비용도 별도로 고려해야 합니다. 계정 한도 설정, 키 권한, 청구 조건 확인 전에는 '6만 원 초과 불가'라고 안내하지 않습니다.

침해가 의심되면 먼저 해당 공급자 키를 폐기하고 유료 생성과 서버를 중단합니다. 사용량·변경 기록을 확인한 후 새 키를 최소 권한으로 발급하며, 지출 한도를 높여 서비스를 복구하지 않습니다. 카드 승인 한도만으로 이미 발생한 서비스 사용료가 사라지는 것은 아니므로 카드 차단만을 비용 방어책으로 삼지 않습니다.

## 확인한 공식 자료

- [OpenAI: 프로젝트 hard spend limit](https://developers.openai.com/api/reference/typescript/resources/admin/subresources/organization/subresources/projects/subresources/spend_limit/methods/update) — 월 USD 기준, 금액은 센트 단위입니다. $10이면 `threshold_amount=1000`이며 적용 상태를 확인합니다. 일반 지출 알림과 구분합니다.
- [Claude: 사용자 지정 spend limit](https://platform.claude.com/docs/en/api/rate-limits#setting-your-own-spend-limit) — 한도 도달 시 요청 거절 동작을 명시합니다.
- [Railway: 비용 제한](https://docs.railway.com/pricing/cost-control) — Compute와 Agent 사용 한도를 따로 설정하며, Compute hard limit 도달 시 서비스가 중단됩니다.
