# AI WORLD 화면 개편

이번 요청의 범위는 첨부 문서에 맞춘 웹사이트 화면 개편이다. 기존 작업 중이던 인증·세계 생성 코드를 활용했으며, 세계 헌법·엔진·DB·유료 모델 설정은 변경하지 않았다.

## 확인한 구조와 요구사항 상태

| 영역 / 추가 구현 지시서 항목 | 확인 결과 |
| --- | --- |
| 기존 구조 분석 (1, 47) | IMPLEMENTED — React/Vite, Node 서버, SQLite, 공개 WORLD API, SSE, 서버 타이머, 인증 및 관리자 빌더 확인 |
| 첫 방문 언어 선택·저장·변경 (2–4) | IMPLEMENTED — 한국어/영어 선택, 브라우저 저장, 로그인 계정 설정 복원 및 기존 locale API 연결 |
| 전체 UI 국제화 (5) | PARTIAL — 새 메인 화면·헤더·로그인·읽기 도구 적용. 기존 상세·관리자 페이지의 전체 번역은 남아 있음 |
| 인증 및 관전 (6–9) | PARTIAL — 이메일 가입/로그인/로그아웃, 세션 유지, 비회원 관전 연결. Google/Kakao 미연결. 서버 관리자 검증은 기존 구조 유지 |
| 관리자·세계 생성·초기 상태 (10–12) | PARTIAL — 기존 관리자/생성 마법사 유지, 이번 디자인 작업에서 운영 기능을 추가 구현하지 않음 |
| 인간 상태·상태 연산 (13–14) | PARTIAL — 기존 캐릭터 생성 모델과 검증 유지. 전체 시뮬레이션 적합성은 이번 UI 작업의 검증 범위 밖 |
| Provider·활성화·fallback·비용/사용량 (15–23) | PARTIAL — 기존 커뮤니티 provider/scheduler/budget 코드 존재. WORLD는 데모 store와 mock narrator를 사용하며 완전한 운영 Agent 연결은 별도 작업 |
| 세계 단일성·이야기 번역 캐시 (24–25) | PARTIAL / NOT_IMPLEMENTED — UI 언어 전환으로 세계를 재생성하지 않음. 이야기 번역 캐시는 미구현; 영어 UI에 원문 표시 안내 |
| 메인·LIVE·지난 이야기 (26–28) | IMPLEMENTED — 기존 Narrator scene API에 연결된 리더, 시간·날씨·상태, 기존 지난 이야기 경로 유지 |
| 지도·이동·공개 범위 (29–31) | PARTIAL — 단일 섬 형태의 미니맵에 공개 캐릭터별 흰 점 표시, 선택한 캐릭터만 빨간 점 표시. 공개 장소별로 점을 묶어 배치한 개략도이며 API의 실제 좌표/공개 여부 스키마 확장, 지리적 이동 애니메이션은 미구현 |
| 인물 상세 (32) | IMPLEMENTED — 새 패널은 이름·소개·공개 상태·위치·최근 행동만 사용, 기존 상세 페이지 연결 |
| Scheduler·tick·잠금·PostgreSQL (33–36) | PARTIAL / NEEDS_CHANGE — 서버 타이머 및 기존 검증 유지. WORLD 메모리 store와 SQLite를 PostgreSQL 운영 구조로 전환하는 작업은 남아 있음 |
| 배포·오류·Narrator 분리 (37–39) | PARTIAL — 기존 Railway 설정과 표현 계층 분리 유지. 실제 운영 LLM 연결은 별도 작업 |
| 실시간 (40) | IMPLEMENTED — 기존 SSE의 worldState/scene/event/runtime에 화면 연결. 새 장면은 알림 후 읽기에 반영 |
| 모바일·UI·관리자 분리 (41–43) | IMPLEMENTED — 이야기 중심의 관전 화면, 모바일 이야기/인물/지도/사건 탭, 로그인한 관리자에만 관리자 링크 표시 |
| 테스트 (44) | PARTIAL — 기존 62개 단위 테스트 + 신규 브라우저 관전/인증/언어 흐름 검증. 미구현 운영 기능의 검증은 남아 있음 |
| 운영 완료 조건 (45) | NOT_IMPLEMENTED — 이번 UI 개편을 문서 전체의 운영 서비스 구현 완료로 간주하지 않음 |
| 기존 기능 보존 (46) | IMPLEMENTED — 기존 핵심 코드 및 작업 중 변경사항 유지, 신규 유료 서비스 없음 |

## 화면 구성

- 차분한 숲색 배경, 명조체 이야기 본문, 민트색 공개 상태 표시.
- 왼쪽 이야기 / 오른쪽 인물·지도·자원 / 아래 사건 타임라인.
- 공개 API 데이터만 사용하며, 섬 미니맵은 개략도임을 표시. 캐릭터나 점을 누르면 해당 점만 빨간색으로 선택되며 다시 누르거나 선택 해제 시 모두 흰색으로 돌아감.
- 모바일에서는 탭으로 보조 패널을 분리해 읽기 공간 확보.
- 로그인 폼은 실제 기존 세션 API 사용. 인증 정보를 localStorage에 저장하지 않음.

## 검증 명령

`npm run typecheck`, `npm run build`, `npm test`, `npx playwright test tests/world-experience.spec.ts --workers=2`

브라우저 테스트는 메모리 DB와 데모 서버에서 실행하며 실제 API 요금을 발생시키지 않는다.
